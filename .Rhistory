?lapply
# list of packages to use:
x <- c('marelac', 'gam', 'seacarb', 'tidyverse', 'lubridate', 'signal', 'cowplot', 'WaveletComp')
load.packages(x)
load.packages = function(a) {
# input: a is a vector of strings.
for (package.name in a){
if (!require(packages.name, character.only = TRUE)){
install.packages(packages.name)
library(packages.name, character.only = TRUE)
}
}
}
load.packages(x)
load.packages = function(a) {
# input: a is a vector of strings.
for (package.name in a){
if (!require(package.name, character.only = TRUE)){
install.packages(package.name)
library(package.name, character.only = TRUE)
}
}
}
load.packages(x)
source('~/Dropbox/sensors/R/functions/sampleFilt.R')
